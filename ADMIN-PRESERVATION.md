# Admin preservation note

The uploaded source snapshot contains public frontend files and admin/editor hooks, but no separate admin application files were included.

This preview does not remove or rewrite those admin/editor hooks. Keep the existing admin application/source separately and merge these public-page changes into the original project before a full Firebase Hosting deployment.
