/* V95: mobile navigation is handled by the existing #hamb + #navLinks + #mobileNavBackdrop system. */
(function(){
  if(document.documentElement) document.documentElement.classList.add('diamondMobileNavV95');
})();
